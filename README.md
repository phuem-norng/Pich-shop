# Pich-shop

Luxury Jewelry POS — dashboard, sales, inventory, customers, suppliers, and staff.

## Local

```bash
npm install
npm start
```

Open http://localhost:3000

## Deploy (Vercel)

Connected via `vercel.json` (static build from `public/`).
